<div>
	<div>
		<a href="{{ post.url }}">{{ post.data.title  }}</a>
		<time
			class="float-right"
			datetime="{{ (post.data.date or post.date) | isoDate }}">
			{{ (post.data.date or post.date) | formatDate }}
		</time>
	</div>
  <hr>
</div>
